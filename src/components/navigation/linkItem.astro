---
const {
    currentPath, 
    href, 
    label,
    customStyle, 
} = Astro.props; 
---
{currentPath.toLowerCase().replace(/\//g,"") === href.toLowerCase().replace(/\//g,"") ? 
<a 
    href={href}
    aria-current="page" 
    class={`w--current whitespace-nowrap ${customStyle ? customStyle : "nav-link w-nav-link"}`}>{label}</a>
    :
<a 
    href={href}
    class={customStyle ? customStyle : "nav-link w-nav-link whitespace-nowrap"}>{label}</a>
}

