---
import PracticeAreaData from "@/data/practice-areas.js"; 
import Panel from "./panel.astro"; 
---
<div class="practice-area-wrap">
    <div class="w-dyn-list">
        <div role="list" class="flex flex-row flex-wrap justify-center">
            {PracticeAreaData && PracticeAreaData.length > 0 && 
                PracticeAreaData.map(area => <Panel {...area} />)
            }
        </div>
    </div>
</div>