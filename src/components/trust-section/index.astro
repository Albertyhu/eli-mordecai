---
import BannerTitleSeparator from "../graphics/banner-title-separator.astro"; 
import TrustReasons from "@/data/trust-reasons.js"; 
import Panel from "./trust-panel.astro"; 
import type {TrustReasonType } from "@/util/interface.js"; 
---
<div class={`help-section relative overflow-hidden h-auto bg-no-repeat bg-cover bg-historyBG_mobile md:bg-historyBG before:w-full before:h-full 
before:absolute before:bg-gradient-to-t before:from-transparent before:to-white before:bottom-0 
before:z-[0]`}>
    <div class="container w-container">
        <div class="section-title">
            <h2 class="section-title-text dark-color-title fadeInFromBottomTag fadeOutIntoBottom z-[3] relative">Why should you trust your case with me?</h2>
            <BannerTitleSeparator 
                customStyle = "mx-auto my-10 fadeInFromBottomTag fadeOutIntoBottom z-[3] relative w-[95px] h-[44px]"/>
            <div class ="md:flex md:flex-row md:gap-5">
                <div
                    class = "flex flex-col"
                >
                    {TrustReasons && TrustReasons.length > 0 && 
                        TrustReasons.map((reason: TrustReasonType, index: number) =><Panel {...reason} {index} />)
                    }
                </div>
                <div class="history-image-wrap w-full h-full h-auto z-[2]">
                    <img 
                        src="/assets/images/blind-justice.webp" 
                        loading="lazy" 
                        decoding="async"
                        alt="blind justice" 
                        class="object-contain w-[264px] h-[778]"/>
                </div>
            </div>
        </div>
    </div>
</div>